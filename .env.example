# MCP Server Configuration
# These environment variables override config.json values

# MCP server name (default: spec-workflow)
MCP_SERVER_NAME=spec-workflow

# MCP package to install (default: npx @pimzino/spec-workflow-mcp@latest)
MCP_PACKAGE=npx @pimzino/spec-workflow-mcp@latest
